# a
imiona = ["Yelyzaveta", "Yuliia", "Sebastian", "Bozena"]
sorted_imiona = sorted(imiona)
print("Posortowane alfabetycznie:", sorted_imiona)

# b
imiona.extend(["Patrik", "Jacek"])
last_imiona = imiona.pop()
print("Ostatnio dodane imie:", last_imiona)

# c
imiona.insert(2, "Jola")
print("Lista po dodaniu imia na pozycji 3:", imiona)

# d
imiona.reverse()
doubled_imiona = imiona * 2
print("Odwrócona i zdublowana lista:", doubled_imiona)
